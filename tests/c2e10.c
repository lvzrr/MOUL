#include "test.h"

int	main(void)
{
	int c = 0;
	while (c < TESTNUM)
	{
		char *c = (char *c)malloc(101 * sizeof(char)); 
		char *o = (char *c)malloc(101 * sizeof(char)); 
		int l = rand % 100;
		int c2 = 0;
		while (c2 < l)
		{

		}
		c++;
	}
	return (0);
}
